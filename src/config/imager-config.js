exports.variants = {
  item: {           // variant
    thumb: {        // preset
      options: {    // preset options
        pool: 5,
        scale: { width: 200, height: 150, type: 'contain' },
        crop: { width: 200, height: 150, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    large: {
      origianl: true  // upload original image without image processing
    },
    androidIcon144X144:{
      options: {    // preset options
        pool: 5,
        scale: { width: 144, height: 144, type: 'contain' },
        crop: { width: 144, height: 144, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    androidIcon192X192:{
      options: {    // preset options
        pool: 5,
        scale: { width: 192, height: 192, type: 'contain' },
        crop: { width: 192, height: 192, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    androidIcon36X36:{
      options: {    // preset options
        pool: 5,
        scale: { width: 36, height: 36, type: 'contain' },
        crop: { width: 36, height: 36, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    androidIcon48X48:{
      options: {    // preset options
        pool: 5,
        scale: { width: 48, height: 48, type: 'contain' },
        crop: { width: 48, height: 48, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    androidIcon72X72:{
      options: {    // preset options
        pool: 5,
        scale: { width: 72, height: 72, type: 'contain' },
        crop: { width: 72, height: 72, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    androidIcon96X96:{
      options: {    // preset options
        pool: 5,
        scale: { width: 96, height: 96, type: 'contain' },
        crop: { width: 96, height: 96, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon144X144:{
      options: {    // preset options
        pool: 5,
        scale: { width: 144, height: 144, type: 'contain' },
        crop: { width: 144, height: 144, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon114X114:{
      options: {    // preset options
        pool: 5,
        scale: { width: 114, height: 114, type: 'contain' },
        crop: { width: 114, height: 114, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon152X152:{
      options: {    // preset options
        pool: 5,
        scale: { width: 152, height: 152, type: 'contain' },
        crop: { width: 152, height: 152, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon180X180:{
      options: {    // preset options
        pool: 5,
        scale: { width: 180, height: 180, type: 'contain' },
        crop: { width: 180, height: 180, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon57X57:{
      options: {    // preset options
        pool: 5,
        scale: { width: 57, height: 57, type: 'contain' },
        crop: { width: 57, height: 57, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon60X60:{
      options: {    // preset options
        pool: 5,
        scale: { width: 60, height: 60, type: 'contain' },
        crop: { width: 60, height: 60, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon72X72:{
      options: {    // preset options
        pool: 5,
        scale: { width: 72, height: 72, type: 'contain' },
        crop: { width: 72, height: 72, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon76X76:{
      options: {    // preset options
        pool: 5,
        scale: { width: 76, height: 76, type: 'contain' },
        crop: { width: 76, height: 76, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIconPreComposed:{
      options: {    // preset options
        pool: 5,
        scale: { width: 159, height: 159, type: 'contain' },
        crop: { width: 159, height: 159, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    appleIcon:{
      options: {    // preset options
        pool: 5,
        scale: { width: 159, height: 159, type: 'contain' },
        crop: { width: 159, height: 159, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebIcon:{
      options: {    // preset options
        pool: 5,
        scale: { width: 519, height: 519, type: 'contain' },
        crop: { width: 519, height: 519, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebIcon96X96:{
      options: {    // preset options
        pool: 5,
        scale: { width: 96, height: 96, type: 'contain' },
        crop: { width: 96, height: 96, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebIcon144X144:{
      options: {    // preset options
        pool: 5,
        scale: { width: 144, height: 144, type: 'contain' },
        crop: { width: 144, height: 144, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebIcon310X310:{
      options: {    // preset options
        pool: 5,
        scale: { width: 310, height: 310, type: 'contain' },
        crop: { width: 310, height: 310, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebFevicon16X16:{
      options: {    // preset options
        pool: 5,
        scale: { width: 16, height: 16, type: 'contain' },
        crop: { width: 16, height: 16, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebFevicon32X32:{
      options: {    // preset options
        pool: 5,
        scale: { width: 519, height: 519, type: 'contain' },
        crop: { width: 519, height: 519, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebFevicon96X96:{
      options: {    // preset options
        pool: 5,
        scale: { width: 96, height: 96, type: 'contain' },
        crop: { width: 96, height: 96, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    WebFevicon:{
      options: {    // preset options
        pool: 5,
        scale: { width: 16, height: 16, type: 'contain' },
        crop: { width: 16, height: 16, x: 0, y: 0 },
        format: 'ico',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    MicrosoftIcon144X144:{
      options: {    // preset options
        pool: 5,
        scale: { width: 144, height: 144, type: 'contain' },
        crop: { width: 144, height: 144, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    MicrosoftIcon150X150:{
      options: {    // preset options
        pool: 5,
        scale: { width: 150, height: 150, type: 'contain' },
        crop: { width: 150, height: 150, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    MicrosoftIcon310X310:{
      options: {    // preset options
        pool: 5,
        scale: { width: 310, height: 310, type: 'contain' },
        crop: { width: 310, height: 310, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
    MicrosoftIcon70X70:{
      options: {    // preset options
        pool: 5,
        scale: { width: 70, height: 70, type: 'contain' },
        crop: { width: 70, height: 70, x: 0, y: 0 },
        format: 'png',
        rotate: 'auto',
      },
      rename: function (file, preset) {
        return;
        // return 'users/1/' + preset + '/' + file.name;
      }
    },
  }
};

exports.storages = {
  local: {
    provider: 'local',
    path: '/tmp',
    mode: 0777
  },
  rackspace: {
    provider: 'rackspace',
    username: process.env.IMAGER_RACKSPACE_USERNAME,
    apiKey: process.env.IMAGER_RACKSPACE_KEY,
    authUrl: 'https://lon.auth.api.rackspacecloud.com',
    region: 'IAD', // https://github.com/pkgcloud/pkgcloud/issues/276
    container: process.env.IMAGER_RACKSPACE_CONTAINER
  },
  amazon: {
    provider: 'amazon',
    key: process.env.IMAGER_S3_KEY,
    keyId: process.env.IMAGER_S3_KEYID,
    container: process.env.IMAGER_S3_BUCKET
  }
}